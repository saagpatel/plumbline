"""Plumbline decision-path scorer (Phase 2).

Deterministic path metrics over a Plumbline trace plus a reference case. The
calibration judge (OPERANT axis-3) layers on top of this deterministic core.
"""
